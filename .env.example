# Logging Configuration
# Options: console, file
LOG_OUTPUT=console

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Log format (text, json)
# Use 'json' for structured logs that can be analyzed retrospectively
LOG_FORMAT=text
